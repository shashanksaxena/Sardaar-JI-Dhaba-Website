# Never commit real values. Rotate any credentials previously shared in chat.
MONGODB_URI=mongodb+srv://<user>:<rotated-password>@<cluster>/<database>
MONGODB_DATABASE=sardaar_ji_dhaba
ADMIN_USERNAME=<admin-username>
# Generate with: node -e "const c=require('node:crypto');const s=c.randomBytes(16).toString('hex');console.log(s+':'+c.scryptSync(process.argv[1],s,64).toString('hex'))" '<new-password>'
ADMIN_PASSWORD_HASH=<salt>:<scrypt-hash>
